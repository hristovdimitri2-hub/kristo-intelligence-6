feat(stripe): wire the new account's webhook end to end (four greens)

The owner authorised the automatic fix, so the webhook is now wired, not
documented:

  * endpoint created in acct_1UFfUcPz7WIGP94b: we_1UGCgzPz7WIGP94b5J2uH2Dj,
    status=enabled, livemode=true, subscribed to checkout.session.completed,
    checkout.session.expired, checkout.session.async_payment_succeeded and
    payment_intent.payment_failed. Stripe returns the signing secret exactly once
    - on creation - and it is now the value of STRIPE_WEBHOOK_SECRET in Render
    (the previous one belonged to the OLD account's endpoint).
  * the Render write is a SINGLE-KEY env update, never a full replace: the key and
    the 27 variables were verified intact afterwards.
  * scripts/wire_stripe_webhook.py keeps that procedure for the next rotation, with
    guards: it refuses a second endpoint for the same URL, aborts before touching
    Render if Stripe did not return a whsec_, and reports the untouched key and
    the variable count.

Evidence, from both sides:
  our log    10.196.80.54 - "POST /api/webhooks/stripe HTTP/1.1" 200
  stripe     checkout.session.expired, pending_webhooks = 0 (delivered)
  the log also shows the Managed Payments fix working: 400 payment_method_types ->
             warning -> retry -> 200, session created.

scripts/stripe_readiness.py: account GREEN, checkout GREEN (303 -> cs_live_,
$29.00 usd, livemode=True verified at Stripe), endpoint GREEN, delivery GREEN ->
exit 0. Tests 278/278 PASS. No change to prices, payTo or the x402 routes.